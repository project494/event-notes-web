PORT=3000
NODE_ENV=development

# Replace with your secure key (run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
AGENT_API_KEY=dev_key_change_me

# For local development
ALLOWED_ORIGINS=http://localhost:3000
